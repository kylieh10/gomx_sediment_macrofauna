# script to run data wrangling scripts

rm(list = ls())
source("scripts/notebook_event.R")

rm(list = ls())
source("scripts/notebook_occurrence.R")

rm(list = ls())
source("scripts/notebook_eMoF.R")